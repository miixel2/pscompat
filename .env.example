# pscompat environment config
# Copy this to .env and fill in values — never commit .env

# Default encoding for all commands (UTF-8 recommended)
PSCOMPAT_ENCODING=UTF8

# Log level: NONE | ERROR | WARN | INFO | DEBUG
PSCOMPAT_LOG_LEVEL=INFO
